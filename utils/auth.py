def login(email, password):
    # check if the db has user with email
    # if user
    #  check if password matches the hashed password
    #  if true login the user
    #     return the user
    #
    # raise ValidationError()
    pass
